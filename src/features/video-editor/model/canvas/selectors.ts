export const canvas = (state: RootState) => state.videoEditor.canvas
export const height = (state: RootState) => state.videoEditor.canvas.height
export const width = (state: RootState) => state.videoEditor.canvas.width
export const scale = (state: RootState) => state.videoEditor.canvas.scale
export const overlays = (state: RootState) => state.videoEditor.canvas.overlays
export const selectedOverlay = (state: RootState) => state.videoEditor.canvas.selectedOverlayId
