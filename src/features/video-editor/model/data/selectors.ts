export const video = (state: RootState) => state.videoEditor.data.video
export const images = (state: RootState) => state.videoEditor.data.images
export const data = (state: RootState) => state.videoEditor.data
